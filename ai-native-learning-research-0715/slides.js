(()=>{const slides=[...document.querySelectorAll('.slide')],deck=document.getElementById('deck'),progress=document.getElementById('progress'),currentNumber=document.getElementById('current-number'),totalNumber=document.getElementById('total-number'),overview=document.getElementById('overview'),overviewGrid=document.getElementById('overview-grid'),notes=document.getElementById('notes'),notesContent=document.getElementById('notes-content'),help=document.getElementById('help-dialog');let current=0,touchStartX=0,touchStartY=0;const clamp=(v,min,max)=>Math.min(Math.max(v,min),max),pad=v=>String(v).padStart(2,'0'),indexFromHash=()=>{const m=location.hash.match(/(?:slide-)?(\d+)/);return m?clamp(Number(m[1])-1,0,slides.length-1):0};function updateNotes(){notesContent.textContent=slides[current].querySelector('.speaker-note')?.textContent.trim()||'本页没有演讲者备注。'}function showSlide(index,updateHash=true){current=clamp(index,0,slides.length-1);slides.forEach((slide,i)=>{const active=i===current;slide.classList.toggle('is-active',active);slide.setAttribute('aria-hidden',String(!active));slide.inert=!active});currentNumber.textContent=pad(current+1);progress.style.width=`${(current+1)/slides.length*100}%`;document.title=`${pad(current+1)} · ${slides[current].dataset.title} — CocoRobo Research`;updateNotes();if(updateHash)history.replaceState(null,'',`#${current+1}`)}function toggleOverview(force){const open=typeof force==='boolean'?force:overview.hidden;overview.hidden=!open;if(open)overview.querySelector(`[data-slide-index="${current}"]`)?.focus()}function toggleNotes(force){const open=typeof force==='boolean'?force:notes.hidden;notes.hidden=!open;if(open)updateNotes()}async function toggleFullscreen(){try{if(!document.fullscreenElement)await document.documentElement.requestFullscreen();else await document.exitFullscreen()}catch(_){}}function buildOverview(){slides.forEach((slide,index)=>{const button=document.createElement('button');button.type='button';button.dataset.slideIndex=String(index);button.innerHTML=`<span>${pad(index+1)}</span><strong>${slide.dataset.title}</strong>`;button.addEventListener('click',()=>{showSlide(index);toggleOverview(false)});overviewGrid.appendChild(button)})}function act(action){if(action==='next')showSlide(current+1);if(action==='prev')showSlide(current-1);if(action==='overview')toggleOverview();if(action==='overview-close')toggleOverview(false);if(action==='fullscreen')toggleFullscreen();if(action==='notes')toggleNotes();if(action==='notes-close')toggleNotes(false);if(action==='help')help.showModal()}document.addEventListener('click',event=>{const target=event.target.closest('[data-action]');if(target)act(target.dataset.action)});document.addEventListener('keydown',event=>{if(help.open)return;if(!overview.hidden&&event.key==='Escape')return toggleOverview(false);if(!notes.hidden&&event.key==='Escape')return toggleNotes(false);if(['ArrowRight','PageDown',' '].includes(event.key)){event.preventDefault();showSlide(current+1)}else if(['ArrowLeft','PageUp'].includes(event.key)){event.preventDefault();showSlide(current-1)}else if(event.key==='Home')showSlide(0);else if(event.key==='End')showSlide(slides.length-1);else if(event.key.toLowerCase()==='o')toggleOverview();else if(event.key.toLowerCase()==='f')toggleFullscreen();else if(event.key.toLowerCase()==='n')toggleNotes();else if(event.key==='?')help.showModal()});deck.addEventListener('touchstart',event=>{touchStartX=event.changedTouches[0].clientX;touchStartY=event.changedTouches[0].clientY},{passive:true});deck.addEventListener('touchend',event=>{const dx=event.changedTouches[0].clientX-touchStartX,dy=event.changedTouches[0].clientY-touchStartY;if(Math.abs(dx)>55&&Math.abs(dx)>Math.abs(dy)*1.25)showSlide(current+(dx<0?1:-1))},{passive:true});addEventListener('hashchange',()=>showSlide(indexFromHash(),false));totalNumber.textContent=pad(slides.length);buildOverview();showSlide(indexFromHash(),false)})();
